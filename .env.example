# Required — verdicts, chat, voice, web search synthesis
OPENAI_API_KEY=

# Optional — better web search (weather works without keys via Open-Meteo; DuckDuckGo is the free fallback)
TAVILY_API_KEY=
SERPER_API_KEY=
BRAVE_SEARCH_API_KEY=

# Optional — disable rule learning in production
# LEARN_FROZEN=true
